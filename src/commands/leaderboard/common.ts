import { createLogger } from "../../utils/logging";

export const logger = createLogger();
logger.setKey("command", "leaderboard");

export const CONTEXT = "flickbot";

export const command = "mclb";
